# ---------------------------------------------------------------------------
# Transformation & development dependencies.
# ---------------------------------------------------------------------------
# Everything needed to rebuild the warehouse from raw data: ingestion, dbt,
# the Postgres driver, the export step, tests and linting.
#
# This file is NOT installed on Streamlit Community Cloud — see requirements.txt
# for the deployment surface.
#
#   pip install -r requirements-dev.txt
# ---------------------------------------------------------------------------

-r requirements.txt

# --- Warehouse / transformation -------------------------------------------
dbt-core==1.12.3
dbt-postgres==1.11.0
psycopg2-binary==2.9.12
sqlalchemy>=2.0,<3.0

# --- Ingestion ------------------------------------------------------------
requests>=2.32,<3.0
python-dotenv>=1.0,<2.0

# --- Quality --------------------------------------------------------------
pytest>=8.0,<9.0
pytest-cov>=5.0,<7.0
ruff>=0.6,<1.0
